# Web Page for Admin
This repository uses **Vibe code** to build the Front-End for the Admin Page.  

This FE is used by internal admins to configure the smart farm and update settings in the database. All the requests will call the APIs via FarmBE repository.

